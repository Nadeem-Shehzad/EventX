
export enum EventStatus {
   DRAFT = 'draft',
   PUBLISHED = 'published',
   CANCELLED = 'cancelled',
   COMPLETED = 'completed',
}

export enum EventVisibility {
   PUBLIC = 'public',
   PRIVATE = 'private',
}

export enum EventType {
   ONLINE = 'online',
   OFFLINE = 'offline',
   HYBRID = 'hybrid',
}